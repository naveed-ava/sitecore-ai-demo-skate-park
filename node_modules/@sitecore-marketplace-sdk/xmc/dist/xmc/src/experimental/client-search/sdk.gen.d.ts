import type { Options as ClientOptions, TDataShape, Client } from '@hey-api/client-fetch';
import type { experimental_Search } from './types.gen';
type GetConfigsData = experimental_Search.GetConfigsData;
export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};
/**
 * Get Configs
 * Get all search configurations for the authenticated client
 */
export declare const getConfigs: <ThrowOnError extends boolean = false>(options?: Options<GetConfigsData, ThrowOnError>) => import("@hey-api/client-fetch").RequestResult<experimental_Search.Config[], experimental_Search.ErrorResponse, ThrowOnError>;
export {};
//# sourceMappingURL=sdk.gen.d.ts.map