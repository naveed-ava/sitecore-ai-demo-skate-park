import type { Options as ClientOptions, TDataShape, Client } from '@hey-api/client-fetch';
import type { Authoring } from './types.gen';
type GraphqlData = Authoring.GraphqlData;
export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};
/**
 * Execute a GraphQL query or mutation
 * Send a GraphQL query or mutation request to the Sitecore Authoring API. Both queries and mutations are supported.
 */
export declare const graphql: <ThrowOnError extends boolean = false>(options: Options<GraphqlData, ThrowOnError>) => import("@hey-api/client-fetch").RequestResult<{
    data?: {
        [key: string]: unknown;
    };
    errors?: Array<{
        message?: string;
        locations?: Array<{
            line?: number;
            column?: number;
        }>;
        path?: Array<string>;
    }>;
}, unknown, ThrowOnError>;
export {};
//# sourceMappingURL=sdk.gen.d.ts.map